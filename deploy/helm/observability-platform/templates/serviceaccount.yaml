{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "observability-platform.serviceAccountName" . }}
  labels:
    {{- include "observability-platform.labels" . | nindent 4 }}
{{- end }}
